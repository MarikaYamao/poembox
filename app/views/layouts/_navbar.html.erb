<header>
  <nav class="navbar navbar-default navbar-static-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'POEMBOX', root_path, class: 'navbar-left' %>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
              <li>
                <%= link_to t('.allpost'), all_path %>
              </li>
              <li>
                <%= link_to t('.postimage'), new_photo_path, remote: true %>
              </li>
  
              <li class="dropdown">
                <%= link_to t('.logout'), logout_path, method: :delete %>
              </li>
            <% else %>
              <li><%= link_to t('.signup'), signup_path, remote: true %></li>
              <li><%= link_to t('.login'), login_path, remote: true %></li>
            <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
